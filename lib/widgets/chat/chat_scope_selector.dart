import 'package:flutter/material.dart';

class ChatScopeSelector extends StatelessWidget {
  const ChatScopeSelector({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
