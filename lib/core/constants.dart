class AppRoutes {
  static const login = '/login';
  static const parentDashboard = '/parent';
  static const childDashboard = '/child';
  static const messages = '/messages';
  static const awards = '/awards';
  static const settings = '/settings';
}

class AppFlags {
  // Phase 1: mock-first (Firebase disabled)
  static const enableFirebase = false;
}

class AppSpacing {
  static const page = 16.0;
  static const cardGap = 12.0;
  static const sectionGap = 18.0;
}
